echo "🔎 Readiness gate (production-readiness playbook, automated subset)..."
node scripts/readiness-gate.mjs || {
  echo "✗ Push blocked by readiness gate. Fix the ✗ items above."
  exit 1
}

echo "✅ Validating (typecheck, lint, format, tests)..."
npm run validate
